# Optional. Without this, LedgerLens still runs — you get the full statistical
# dashboard, just without the AI-written narrative section.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
